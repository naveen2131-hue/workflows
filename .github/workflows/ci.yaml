name: Defectdojo - endorlabs

on:
  push:
    branches:
      - "**
      
jobs:
  endorlabs:
    runs-on: ubuntu-latest

    steps:
      
      - name: checkout code
        uses: actions/checkout@v6
